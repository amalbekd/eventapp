
# 🎓    eventapp (Mobile API)

Backend-часть платформы для школьников и организаторов. Позволяет управлять профилями, событиями и заявками на участие.

## 📍 Основная информация
- **Base URL:** `http://localhost:8080`
- **Формат данных:** `application/json`
- **Аутентификация:** JWT Bearer Token

---

## 🔐 Аутентификация (JWT)

Для доступа к защищенным маршрутам (`/api/*`) необходимо передавать токен в заголовках каждого запроса:

**Header:**
```http
Authorization: Bearer YOUR_JWT_TOKEN
```

> **Важно:** Токен выдается при успешной регистрации или логине. Срок жизни — 72 часа.

---

## 📡 Эндпоинты API

### 1. Авторизация и профиль
| Метод | Путь | Описание | Защищен? |
|:--- |:--- |:--- |:--- |
| **POST** | `/auth/register` | Регистрация (email, password, full_name) | 🔓 Нет |
| **POST** | `/auth/login` | Вход (email, password) -> возвращает `token` | 🔓 Нет |
| **GET** | `/api/me` | Данные текущего пользователя по токену | 🔒 Да |

### 2. Управление событиями (Events)
| Метод | Путь | Описание | Защищен? |
|:--- |:--- |:--- |:--- |
| **GET** | `/api/events` | Получить список всех событий | 🔒 Да |
| **POST** | `/api/events` | Создать новое событие | 🔒 Да |
| **GET** | `/api/events/:id` | Детальная информация о событии | 🔒 Да |
| **PUT** | `/api/events/:id` | Полное обновление события (только создатель) | 🔒 Да |
| **DELETE** | `/api/events/:id` | Удаление события (только создатель) | 🔒 Да |

### 3. Заявки на участие (Registrations)
| Метод | Путь | Описание | Защищен? |
|:--- |:--- |:--- |:--- |
| **POST** | `/api/events/:id/apply` | Подать заявку на участие в событии | 🔒 Да |
| **GET** | `/api/my/events` | Список событий, на которые записан текущий юзер | 🔒 Да |
| **GET** | `/api/events/:id/participants` | Список всех заявок на ивент (для организатора) | 🔒 Да |
| **PATCH** | `/api/registrations/:id/status` | Изменить статус заявки (`approved`, `rejected`) | 🔒 Да |

---

## 📋 Примеры объектов

### Событие (Event)
```json
{
  "id": 1,
  "title": "Олимпиада по физике",
  "description": "Городская олимпиада...",
  "category": "STEM",
  "format": "Offline",
  "city": "Almaty",
  "date": "2024-05-15T10:00:00Z",
  "organizer_id": 1
}
```

### Заявка (Registration)
```json
{
  "id": 5,
  "user_id": 10,
  "event_id": 1,
  "status": "pending", // Возможные: pending, approved, rejected
  "event": { ... },    // Присутствует в GET /api/my/events
  "user": { ... }      // Присутствует в GET /api/events/:id/participants
}
```

---

## ⚠️ Обработка ошибок

Сервер возвращает стандартные HTTP-коды:
- `200 / 201` — Успех.
- `400 Bad Request` — Ошибка валидации или неверные данные.
- `401 Unauthorized` — Токен отсутствует, истек или невалиден.
- `403 Forbidden` — Попытка изменить чужой ивент или статус без прав организатора.
- `409 Conflict` — Попытка повторной регистрации на один и тот же ивент.

**Формат ошибки:**
```json
{
  "error": "Описание причины ошибки на английском"
}
```

---

## 🛠 Как запустить локально (для фронтенда)
1. Установи Docker.
2. В корне проекта выполни `docker-compose up -d` (запустит базу данных).
3. Создай `.env` (см. `.env.example`).
4. Выполни `go run cmd/app/main.go`.
5. API будет доступен на порту `8080`.

---

### Подсказка для фронтенда:
Для изменения статуса заявки используйте **PATCH** `/api/registrations/{id}/status` с телом:
```json
{
  "status": "approved" 
}
```
*(где `id` — это ID заявки из списка участников).*
