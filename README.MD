
# 🎓    eventapp (Mobile API)

Это серверная часть мобильного приложения для учеников, родителей и организаторов. Система позволяет искать образовательные возможности (олимпиады, ивенты, кружки), регистрироваться на них и формировать портфолио достижений.

## 🚀 Стек технологий
* **Язык:** Go (Golang) 1.25+
* **Фреймворк:** Gin Gonic (HTTP)
* **База данных:** PostgreSQL 15
* **ORM:** GORM
* **Аутентификация:** JWT (JSON Web Tokens)
* **Контейнеризация:** Docker / Docker Compose

---

## 🛠 Как запустить проект

### 1. Подготовка
Убедись, что у тебя установлены:
* **Go**
* **Docker** и **Docker Compose**

### 2. Клонирование и настройка
```bash
git clone <ссылка_на_репозиторий>
cd ed-backend
```

Создай файл **`.env`** в корне проекта и скопируй туда настройки:
```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=eventapp
JWT_SECRET=super_secret_key_for_hackathon
PORT=8080
```

### 3. Запуск базы данных (через Docker)
Бэкенд использует PostgreSQL в контейнере. Чтобы поднять базу:
```bash
docker-compose up -d
```

### 4. Запуск сервера
```bash
go mod tidy          # Установка зависимостей
go run cmd/app/main.go
```
Сервер будет доступен по адресу: `http://localhost:8080`

---

## 🔑 Аутентификация
В проекте используется **JWT Bearer Token**.
1. Получи токен через эндпоинты `/auth/register` или `/auth/login`.
2. Передавай его в каждом защищенном запросе в заголовок:
   `Authorization: Bearer <ваш_токен>`

---

## 📡 Основные API Эндпоинты

### 🔓 Публичные (Auth)
* `POST /auth/register` — Регистрация нового аккаунта.
* `POST /auth/login` — Вход (возвращает токен).

### 🔒 Защищенные (Требуют Token)
* `GET /api/me` — Получение данных текущего пользователя (профиль).
* `GET /api/events` — Список всех доступных событий/олимпиад.
* `POST /api/events` — Создание нового события (только для организаторов).
* `GET /api/events/:id` — Детальная информация о конкретном событии.

---

## 📂 Структура проекта (для справки)
* `/api` — Обработчики HTTP запросов (Handlers).
* `/service` — Бизнес-логика (шифрование, генерация токенов).
* `/repository` — Работа с базой данных (SQL/GORM).
* `/models` — Описание структур данных (User, Event).
* `/cmd/app` — Точка входа в приложение.

---

## 📝 Примечания для фронтенда
1. **Типы данных:** Все числовые ID приходят как `uint` (в JSON это обычные числа).
2. **Формат дат:** Даты передаются в формате ISO 8601 (например, `2024-06-15T09:00:00Z`).
3. **Ошибки:** В случае ошибки сервер возвращает JSON вида `{"error": "текст ошибки"}` со соответствующим HTTP кодом (400, 401, 404, 500).

---

### Разработчик бэкенда
* **Язык:** Go
* **База:** PostgreSQL
* **Статус:** MVP в процессе разработки (День 2 из 5)
